ご提示いただいたファイル（`index.html`, `style.css`, `script.js`）の内容に基づき、将来の見直しや再設計に役立つ**「基本設計・仕様書」**を作成しました。
この内容は、アプリの目的、技術構成、データの持ち方、そして特に注力した**セキュリティ対策の意図**を網羅しています。`specification.md` というファイル名で保存することを想定しています。
---

```markdown
# ガントチャートWebアプリ (STL-schedule) 仕様書

## 1. アプリケーション概要
本アプリケーションは、サーバーサイドを必要とせず、ブラウザ上ですべて完結する（クライアントサイド動作）ガントチャート・日程表作成ツールである。
「計画モード」と「進捗モード」を切り替えながら、スケジュールの作成と予実管理を行うことができる。

### コンセプト
* **セキュア**: IPAガイドラインおよびCWE Top 25 (2025) を考慮した堅牢な設計。
* **スタンドアローン**: HTML/CSS/JSファイルのみで動作し、インストール不要。
* **ローカル保存**: データはブラウザ内（IndexedDB）に自動保存される。

---

## 2. 技術スタックと構成

### 2.1 技術要素
* **HTML5**: 構造定義。CSP (Content Security Policy) による保護。
* **CSS3**: Flexbox/Gridレイアウト、CSS変数によるテーマ管理。
* **Vanilla JavaScript (ES6+)**: フレームワーク（React/Vue等）不使用。DOM操作およびロジック制御。

### 2.2 ファイル構成
* `index.html`: アプリケーションのUI構造およびセキュリティ設定。
* `style.css`: デザイン定義。レスポンシブ対応（簡易）、印刷設定。
* `script.js`: アプリケーションの全ロジック、データ管理、イベントハンドラ。

### 2.3 データ保存方式
1.  **IndexedDB**: メインの保存先（データベース名: `GanttAppDB`, ストア名: `appData`）。
2.  **LocalStorage**: IndexedDBが使用できない環境（Privateブラウズ等）でのフォールバック先。
3.  **JSONファイル**: ユーザーによる手動のエクスポート/インポート機能。

---

## 3. セキュリティ設計 (重要)
本アプリは外部からの攻撃（XSS, DoS等）を防ぐため、以下の対策を実装済みである。**再設計時はこれらの制限を解除しないよう注意すること。**

### 3.1 入力・リソース制限 (CWE-400 / DoS対策)
ブラウザのクラッシュを防ぐため、`script.js` 内の `LIMITS` 定数で厳格な制限を設けている。

| 項目 | 制限値 | 理由 |
| :--- | :--- | :--- |
| **計画名** | 50文字 | レイアウト崩れ防止 |
| **項目名・ラベル** | 100文字 | 長文による描画負荷軽減 |
| **メモ欄** | 5000文字 | 文字列操作によるフリーズ防止 |
| **タスク行数** | 500行 | DOM要素数の爆発的増加防止 |
| **1行あたりの区間数** | 360個 | 描画ループの負荷軽減（1年分を想定） |

### 3.2 クロスサイトスクリプティング (XSS) 対策
* **DOM操作**: `innerHTML` の使用を禁止し、`textContent` / `innerText` のみを使用。
* **CSP設定**: `index.html` にて、インラインスクリプトおよび外部ソースの読み込みを禁止。
    * `script-src 'self'`
* **ペースト浄化**: `contenteditable` 領域（メモ欄など）へのペースト時、リッチテキスト（HTMLタグ）を除去し、プレーンテキストのみを挿入するイベントリスナーを実装。

### 3.3 データ整合性と汚染防止 (CWE-20 / CWE-1321対策)
* **JSONインポート時の検証**:
    * 拡張子 (`.json`) および MIMEタイプチェック。
    * 必須プロパティ (`tasks` 配列等) の存在チェック。
* **ホワイトリスト方式の復元**:
    * 読み込んだデータ (`data`) をそのまま使用せず、必要なプロパティのみを型チェック・文字数カット(`truncate`)した上で、新しいオブジェクトにコピーして取り込む。これにより**プロトタイプ汚染**を防ぐ。

### 3.4 クリックジャッキング対策
* **CSP設定**: `frame-ancestors 'none'` を指定し、他サイトの `<iframe>` 内での表示を禁止。

---

## 4. データ構造 (Schema)
アプリケーション内部で保持するデータオブジェクト (`appData`) の構造。

```json
{
  "projectName": "String (max 50 chars)",
  "settings": {
    "startDate": "YYYY-MM-DD",
    "endDate": "YYYY-MM-DD",
    "holidays": ["YYYY-MM-DD", ...]
  },
  "memo": "String (max 5000 chars)",
  "tasks": [
    {
      "id": "String (unique)",
      "label1": "String (項目1)",
      "label2": "String (項目2)",
      "label3": "String (時間)",
      "isDone": Boolean,
      "isHidden": Boolean,
      "segments": [
        {
          "id": "String",
          "startDate": "YYYY-MM-DD",
          "endDate": "YYYY-MM-DD",
          "type": "range" | "point",
          "label": "String",
          "progressEndDate": "YYYY-MM-DD" | null,
          "dailyValues": { "YYYY-MM-DD": Number, ... }
        }
      ]
    }
  ]
}

```

---

## 5. UI/UX 設計仕様

### 5.1 レイアウト

* **左右分割**: 左側にタスク情報（行ヘッダー）、右側にタイムライン（ガントチャート）を配置。
* **同期スクロール**: 右側のエリアは横スクロール可能。

### 5.2 特記事項（スタイル）

* **計画名入力欄**: `style.css` にて `#projectNameInput` に対し `min-width: 450px` を強制指定している。これは全角約30文字を確実に見せるための仕様であり、**変更時は可視範囲への影響を考慮すること。**
* **コンテキストメニュー**: タスク行の左側（`.left-row`）を右クリックすることで独自のメニューを表示。

### 5.3 操作仕様

* **タスク作成**: 「＋行を追加」ボタン。
* **セグメント作成**: タイムライン上のセルをクリック（始点）→別のセルをクリック（終点）。
* 重複チェック: 1日あたり最大3つまで重なりを許容。


* **進捗入力**: 「進捗モード」にてセグメントを選択後、日付セルをクリックして進捗線を記録。
* **並び替え**: 左側のグリップアイコンをドラッグ＆ドロップで行の入れ替えが可能。

---

## 6. 再設計・保守時の注意事項

1. **制限値の変更**: `LIMITS` 定数を緩和する場合、必ずパフォーマンステスト（大量データ入力時の挙動確認）を行うこと。
2. **HTML構造の変更**: JS側で `children[1]` などのインデックスによるDOM取得を行っている箇所があるため、HTML階層を変更する際は `script.js` の `syncDataModel` や `addTaskRow` 内のロジック修正が必須となる。
3. **ブラウザ互換性**: 現代のブラウザ（Chrome/Edge/Firefox/Safari）を想定。IE対応は考慮されていない。

```

```